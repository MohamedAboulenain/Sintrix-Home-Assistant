# Inventory — Areas

> Curated summary, maintained by Sintrix via `/inventory`. NOT a full entity dump — query
> ha-mcp live (`ha_get_overview`, `ha_search_entities`) for current detail.

| Area / Room | Zone / Floor | Notes |
|-------------|--------------|-------|
| _(unset)_   |              |       |

_Last refreshed: never (run `/inventory`)._
